### NOTE: THIS STILL NEEDS TO BE AUTOMATED ###
#
### ESSENTIALS

## Utilities

# Install VBoxGuestAdditions

# Basics for editing
sudo apt-get install vim-gnome #gvim
sudo apt-get install nautilus-open-terminal # for ubuntu

# Python stuff
sudo apt-get install python-pip
sudo pip install virtualenv

# Chrome
sudo apt-get install google-chrome

## Themes

# In synaptic...

# gnome-themes --> mint, etc
# shiki-colors

# this might not be necessary given shiki-colors
sudo apt-get install gnome-colors
# http://code.google.com/p/gnome-colors/

#FreshDark
# http://hcalves.deviantart.com/art/Fresh-Dark-Gnome-Theme-2-1-95737856
# (put the unpacked directory in ~/.themes)
#also, you'll need this for stop error messages in the appearance box
sudo apt-get install gtk2-engines-aurora

# New Wave is on the netbook, and it works pretty well too.

# For netbook...
sudo apt-get install docky
# uses HUD theme with intellihide, icon size 52, zoom 200%
# also ensure it runs on startup/login
# then remove bottom gnome panel

### SERVERS

## Git/SSH

sudo apt-get install git

# then set up ssh to github
# http://help.github.com/linux-set-up-git/
# get settings from the github settings repo
# (follow the instructions in the README file, readable online)

# set up ssh to webfaction
# http://docs.webfaction.com/user-guide/access.html

### PROJECTS

# http://docs.webfaction.com/software/git.html
git clone chrispsn@chrispsn.webfactional.com:webapps/git/repos/<repo name>.git
# follow each project's instructions from there...
